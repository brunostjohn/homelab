apiVersion: v1
kind: Service
metadata:
  name: readarr
  namespace: entertainment
spec:
  selector:
    app: readarr
  ports:
    - port: 8787
      targetPort: 8787
      name: http
      protocol: TCP
    # - port: 9707
    #   targetPort: 9707
    #   name: monitoring
    #   protocol: TCP
  type: ClusterIP
