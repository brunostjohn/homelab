apiVersion: v1
kind: Service
metadata:
  name: radarr
  namespace: entertainment
spec:
  selector:
    app: radarr
  ports:
    - port: 7878
      targetPort: 7878
      name: http
      protocol: TCP
    # - port: 9707
    #   targetPort: 9707
    #   name: monitoring
    #   protocol: TCP
  type: ClusterIP
