apiVersion: v1
kind: Service
metadata:
  name: homeassistant-svc
  namespace: homeassistant
  labels:
    app: homeassistant
spec:
  ports:
    - port: 80
      name: http
      protocol: TCP
      targetPort: 80
  selector:
    app: homeassistant
  type: ClusterIP
