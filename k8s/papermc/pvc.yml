kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: papermc
  namespace: minecraft
spec:
  storageClassName: floof-nfs-csi
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
