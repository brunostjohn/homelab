kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: papermc
  namespace: minecraft
spec:
  storageClassName: nfs-jabberwock-subpath
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
