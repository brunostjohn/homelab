apiVersion: v1
kind: Service
metadata:
  name: plex
  namespace: entertainment
spec:
  selector:
    app: plex
  ports:
    - port: 32400
      targetPort: 32400
      name: http
      protocol: TCP
    - port: 9707
      targetPort: 9707
      name: monitoring
      protocol: TCP
  type: ClusterIP
