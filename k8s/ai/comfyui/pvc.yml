apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: comfyui-pvc
  namespace: ai
spec:
  resources:
    requests:
      storage: 10Gi
  storageClassName: floof-iscsi-csi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
