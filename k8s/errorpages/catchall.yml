apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: catch-all
  namespace: kube-system
spec:
  entryPoints:
    - https
  routes:
    - match: "PathPrefix(`/`)"
      kind: Rule
      priority: 1
      services:
        - name: errorpages
          kind: Service
          port: 80
