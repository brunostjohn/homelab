apiVersion: apps/v1
kind: Deployment
metadata:
  name: manyfold
  namespace: 3dprint
spec:
  selector:
    matchLabels:
      app: manyfold
  template:
    metadata:
      labels:
        app: manyfold
    spec:
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: manyfold-data-pvc
        - name: config
          persistentVolumeClaim:
            claimName: manyfold-config-pvc
      securityContext:
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
        - name: manyfold
          image: lscr.io/linuxserver/manyfold:latest
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "500m"
          securityContext:
            allowPrivilegeEscalation: false
            runAsUser: 0
          env:
            - name: PGID
              value: "1001"
            - name: PUID
              value: "1001"
            - name: DATABASE_URL
              value: postgresql://postgres:postgres@postgres-postgresql.databases.svc.cluster.local:5432/manyfold
            - name: REDIS_URL
              value: redis://redis@redis-master.databases.svc.cluster.local:6379/0
            - name: SECRET_KEY_BASE
              valueFrom:
                secretKeyRef:
                  name: manyfold
                  key: secret_key_base
          ports:
            - containerPort: 3214
              name: http
              protocol: TCP
          startupProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 1920
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 5
          volumeMounts:
            - name: data
              mountPath: /libraries
            - name: config
              mountPath: /config
