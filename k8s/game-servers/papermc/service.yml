apiVersion: v1
kind: Service
metadata:
  name: papermc
  namespace: game-servers
spec:
  selector:
    app: papermc
  ports:
    - port: 25565
      targetPort: 25565
      protocol: TCP
      name: minecraft
  type: LoadBalancer
  loadBalancerIP: 10.0.2.25
