apiVersion: v1
kind: Service
metadata:
  name: truecommand
  namespace: truecommand
spec:
  selector:
    app: truecommand
  ports:
    - port: 80
      targetPort: 80
      name: http
      protocol: TCP
