apiVersion: v1
kind: Service
metadata:
  name: lidarr
  namespace: entertainment
spec:
  selector:
    app: lidarr
  ports:
    - port: 8686
      targetPort: 8686
      name: http
      protocol: TCP
    - port: 9707
      targetPort: 9707
      name: monitoring
      protocol: TCP
  type: ClusterIP
