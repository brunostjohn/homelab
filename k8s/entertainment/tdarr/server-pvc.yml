apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tdarr-server-pvc
  namespace: entertainment
spec:
  resources:
    requests:
      storage: 5Gi
  storageClassName: longhorn
  accessModes:
    - ReadWriteOnce
