apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: octoprint-pvc
  namespace: octoprint
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
  storageClassName: longhorn
