apiVersion: v1
kind: Service
metadata:
  name: octoprint-service
  namespace: octoprint
spec:
  selector:
    app: octoprint
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
  type: ClusterIP
