apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: affine-data
  namespace: productivity
spec:
  resources:
    requests:
      storage: 50Gi
  storageClassName: floof-iscsi-csi
  accessModes:
    - ReadWriteOnce
