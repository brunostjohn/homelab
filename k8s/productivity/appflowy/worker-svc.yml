apiVersion: v1
kind: Service
metadata:
  name: appflowy-worker
  namespace: productivity
  labels:
    app: appflowy-worker
spec:
  selector:
    app: appflowy-worker
  ports:
    - port: 4001
      targetPort: http
      protocol: TCP
      name: http
