apiVersion: v1
kind: Service
metadata:
  name: appflowy-admin-frontend
  namespace: productivity
spec:
  selector:
    app: appflowy-admin-frontend
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
