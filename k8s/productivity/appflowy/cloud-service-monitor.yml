apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: appflowy-cloud
  namespace: productivity
spec:
  selector:
    matchLabels:
      app: appflowy-cloud
  endpoints:
    - port: http
      path: /api/metrics
