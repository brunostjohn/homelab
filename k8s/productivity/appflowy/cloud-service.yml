apiVersion: v1
kind: Service
metadata:
  name: appflowy-cloud
  namespace: productivity
  labels:
    app: appflowy-cloud
spec:
  selector:
    app: appflowy-cloud
  ports:
    - port: 3000
      targetPort: http
      protocol: TCP
      name: http
