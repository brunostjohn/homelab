apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: appflowy-gotrue
  namespace: productivity
spec:
  selector:
    matchLabels:
      app: appflowy-gotrue
  endpoints:
    - port: metrics
      path: /
