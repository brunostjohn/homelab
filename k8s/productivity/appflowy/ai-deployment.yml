apiVersion: apps/v1
kind: Deployment
metadata:
  name: appflowy-ai
  namespace: productivity
  annotations:
    reloader.stakater.com/auto: "true"
    secrets.infisical.com/auto-reload: "true"
spec:
  selector:
    matchLabels:
      app: appflowy-ai
  template:
    metadata:
      labels:
        app: appflowy-ai
        collection: appflowy
    spec:
      containers:
        - name: appflowy-ai
          image: docker.io/appflowyinc/appflowy_ai:0.6.16
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          ports:
            - containerPort: 5001
              protocol: TCP
              name: http
          startupProbe:
            httpGet:
              path: /
              port: http
          failureThreshold: 30
          periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: http
          livenessProbe:
            httpGet:
              path: /
              port: http
          envFrom:
            - configMapRef:
                name: appflowy-ai-config
            - secretRef:
                name: appflowy-ai-secrets
