apiVersion: v1
kind: Service
metadata:
  name: esphome
  namespace: smarthome
spec:
  selector:
    app: esphome
  ports:
    - port: 6052
      targetPort: http
    - port: 80
      targetPort: http
  type: LoadBalancer
