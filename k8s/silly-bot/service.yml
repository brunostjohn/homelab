apiVersion: v1
kind: Service
metadata:
  name: silly-bot
  namespace: default
spec:
  selector:
    app: silly-bot
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
