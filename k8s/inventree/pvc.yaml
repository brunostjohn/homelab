apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: inventree-pvc
  namespace: productivity
spec:
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteMany
  storageClassName: floof-nfs-csi
