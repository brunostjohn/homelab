- name: Bootstrap k3s
  hosts: all
  tasks:
    - name: Run k3s install script
      shell: "curl -sfL https://get.k3s.io | K3S_URL=https://s1.nixos.nodes.zefirscloud.local:6443 K3S_TOKEN={{ k3s_token }} sh -"
      become_user: root
      become_method: sudo
      tags: k3s
