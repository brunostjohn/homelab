services:
  server:
    container_name: onedev
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /mnt/jabberwock/githost:/opt/onedev
    networks:
      - rev_proxy
    hostname: onedev
    image: 1dev/server

networks:
  rev_proxy:
    name: reverse_proxy_network
    external: true
