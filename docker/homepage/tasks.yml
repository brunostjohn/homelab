version: 3

tasks:
  produce:
    aliases: ["p"]
    desc: "Replace secrets from .env file and produce config files"
    cmds:
      - rm -rf ../../dist/homepage-config
      - mkdir -p ../../dist/homepage-config
      - cp -r ./config/* ../../dist/homepage-config/
      - fd . '../../dist/homepage-config' -t f | xargs -I {} envsubst -o {} -i {} -no-unset -no-empty -fail-fast
